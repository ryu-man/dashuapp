{
	"name": "dashuapp",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
    "build": "cd client && yarn build",
		"install:client": "cd client && yarn install",
		"heroku-postbuild": "yarn install:client && yarn build",
		"start": "node index.js",
		"nodemon": "nodemon index",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "abdelhalim riache",
	"license": "ISC",
	"dependencies": {
		"bcrypt": "^5.0.0",
		"body-parser": "^1.19.0",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"jsonwebtoken": "^8.5.1",
		"mongoose": "^5.11.11",
		"passport": "^0.4.1"
	},
	"devDependencies": {
		"nodemon": "^2.0.7"
	},
	"engines": {
		"node": "14.x"
	}
}
